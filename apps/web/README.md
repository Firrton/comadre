# @comadre/web

Next.js 15 (App Router). Dos jobs:
1. **Landing** — marketing + waitlist.
2. **Admin dashboard** (`/admin`) — internal team use only (Privy gate por wallet allowlist).

```bash
bun run dev   # http://localhost:3000
```
