# @comadre/api

Public API service. Bun + Hono. Auth via Privy JWT. Builds unsigned Solana txs for clients to sign.

**Port:** 3001
**Routes:** `/api/v1/*` and `/webhooks/*`

See [../../docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md) for full design.
