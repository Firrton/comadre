# @comadre/db

Drizzle ORM + Postgres (Supabase). Tablas materializadas desde eventos on-chain por `@comadre/indexer`.

```bash
bun run generate   # genera migration desde cambios en schema.ts
bun run migrate    # aplica migrations a DB
bun run studio     # UI de tablas
```
