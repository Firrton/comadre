# Flat node_modules (like npm/bun) — maximizes compat with the bun runtime,
# turbo, and avoids pnpm strictness breaking on phantom deps in this monorepo.
# Can be tightened to the default isolated linker later once deps are clean.
node-linker=hoisted
strict-peer-dependencies=false
